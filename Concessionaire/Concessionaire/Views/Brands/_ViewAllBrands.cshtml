@model IEnumerable<Concessionaire.Data.Entities.Brand>

<table class="table table-striped table-responsive-md table-bordered smfont"  id="BrandsTable">
    <colgroup>
        <col span="1" />
        <col span="1" />
    </colgroup>
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VehiclesNumber)
            </th>

            <th>
                <a onclick="showInPopup('@Url.Action("AddOrEdit", "Brands", new { id = 0 }, Context.Request.Scheme)' , 'Nueva Marca')" class="btn btn-sm btn-primary text-white"><i class="fa fa-plus-circle"></i> Marca</a>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VehiclesNumber)
            </td>
            <td>
                <a onclick="showInPopup('@Url.Action("AddOrEdit", "Brands", new { id = item.Id }, Context.Request.Scheme)' , 'Actualizar Marca')" class="btn btn-sm btn-success"><i class="fa fa-gear"></i> </a>
                <button data-id="@item.Id" class="btn btn-sm btn-danger deleteItem" data-bs-toggle="modal" data-bs-target="#deleteDialog"><i class="fa fa-trash"></i></button>
            </td>
        </tr>
        }
    </tbody>
</table>
